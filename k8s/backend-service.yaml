apiVersion: v1
kind: Service
metadata:
  name: backend-__ENV__
spec:
  type: ClusterIP
  selector:
    app: backend
    env: __ENV__
    expose: "true"
  ports:
    - port: 9000
      targetPort: 9000
