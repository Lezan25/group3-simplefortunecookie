services:
  backend:
    image: aljazkni/simplefortunecookie-backend:latest
    ports:
      - "9000:9000"

  frontend:
    image: aljazkni/simplefortunecookie-frontend:latest
    ports:
      - "8080:8080"
    depends_on:
      - backend